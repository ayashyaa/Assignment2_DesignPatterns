package app.gui.product;

// Specific object (product) - Windows Button
public class WinCheckbox implements Checkbox {
    @Override
    public void render() {
        System.out.println("GUI: Отрисовываем флажок в стиле Windows.");
    }
}